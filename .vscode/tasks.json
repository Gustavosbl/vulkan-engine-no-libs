{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Windwos Build",
            "command": "${workspaceFolder}/build.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}